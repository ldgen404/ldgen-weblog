<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ldgen.weblog.mapper.StatisticsArticlePvMapper">

    <select id="selectPvTotalCount" resultType="java.lang.Long">
        select ifnull(sum(pv_count), 0)
        from statistics_article_pv
    </select>

    <insert id="increasePvCountByDate">
        insert into statistics_article_pv (pv_date, pv_count, create_time, update_time)
        values (#{pvDate}, #{pvCount}, now(), now())
        on duplicate key update
            pv_count = pv_count + values(pv_count),
            update_time = now()
    </insert>

</mapper>
